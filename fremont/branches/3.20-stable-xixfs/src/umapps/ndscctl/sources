TARGETNAME=libndasscictl
TARGETTYPE=LIBRARY
TARGETPATH=obj

BUILD_PRODUCES=libndasscsictl

INCLUDES=$(NDAS_INC_PATH)
C_DEFINES=-DUNICODE -D_UNICODE

!IF $(FREEBUILD)
USE_MSVCRT=1
!ELSE
DEBUG_CRTS=1
!ENDIF

# publish lib files to the project root
PASS1_PUBLISH= {$(O)\$(TARGETNAME).lib=$(NDAS_LIB_PATH)\$(TARGETNAME).lib}

SOURCES=ndscctl.c
