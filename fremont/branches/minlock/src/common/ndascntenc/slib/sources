TARGETNAME=libndascntenc
TARGETTYPE=LIBRARY
TARGETPATH=obj

BUILD_PRODUCES=libndascntenc

INCLUDES=$(NDAS_INC_PATH);$(NDAS_SRC_PATH)\inc\public;$(PSDK_INC_PATH)

C_DEFINES=/DUNICODE /D_UNICODE

!IF $(FREEBUILD)
USE_MSVCRT=1
!ELSE
USE_LIBCMT=1
DEBUG_CRTS=1
!ENDIF

SOURCES=..\ndascntenc.cpp

PASS1_PUBLISH= {$(O)\$(TARGETNAME).lib=$(NDAS_LIB_PATH)\$(TARGETNAME).lib}
