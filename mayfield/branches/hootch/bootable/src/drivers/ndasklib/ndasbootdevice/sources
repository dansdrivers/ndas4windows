!INCLUDE ..\sources.inc

TARGETNAME=libndaskbootdevice
TARGETTYPE=DRIVER_LIBRARY
TARGETPATH=obj

BUILD_PRODUCES=libndaskbootdevice


#MULTIBOOT=multiboot

C_DEFINES = -D__NDASBOOT__ $(C_DEFINES)
!IFDEF MULTIBOOT
C_DEFINES = -D__DEVIDED_MULTI_BOOT__ $(C_DEFINES)
!ENDIF

SOURCES_USED=..\sources.inc
SOURCES=$(SOURCES)
		
PASS1_PUBLISH= {$(O)\$(TARGETNAME).lib=$(NDAS_DRIVER_LIB_PATH)\$(TARGETNAME).lib}
